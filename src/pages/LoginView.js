import { Helmet } from 'react-helmet';

export default function LoginView() {
    return (
        <div>
            <Helmet>
                <title>Login</title>
            </Helmet>
            <h2>Log in</h2>
        </div>
    )
};